# Requires curl and read access to /var/run/docker.sock for the Zabbix agent 2 user.
UserParameter=docker.containers.labels.raw,curl --silent --show-error --fail --max-time 10 --unix-socket /var/run/docker.sock 'http://localhost/containers/json?all=true'
